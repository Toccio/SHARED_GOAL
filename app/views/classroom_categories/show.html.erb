<div class="index-content">
  <div class="sidebar">
    <div class="text"><%= link_to "Categories", lists_path %></div>
    <ul>
      <% @lists.each do |i| %>
        <li><%= link_to i.name %></li>
      <% end %>
    </ul>
  </div>
  <div class="category-cards">
    <div class="cards-title"><h1><strong>Music</strong></h1></div>
    <div class="cards-display">
        <div class="index-card">
          <% @classroom_categories.each do |classroom_category| %>
            <%= link_to "",classroom_category_classrooms_path(ClassroomCategory.first), class: "card-link" %>
            <h2> <%= classroom_category.name %> </h2>
          <% end %>
        </div>
    </div>
  </div>
</div>
