<%= simple_form_for @chatroom do |f| %>
    <%= f.input :name, class: "name-form" %>
    <button class="gray-button form-actions w-100 rounded"> <%= f.button :submit, "New chatroom!" %> </button>
<% end %>
